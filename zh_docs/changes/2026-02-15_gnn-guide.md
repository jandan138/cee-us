# 2026-02-15 — GNN 导读（6 篇）

- Commit: 8306a67

## 背景

希望以中文为主，为 GNN 新手提供“从概念到代码到可运行实验”的导读，读完可以理解：

- GNN 的基本工作方式（message passing / aggregation）
- 本项目的对象中心动力学建模方式
- 本项目中 GNN 相关代码结构与入口

## 改动

- 新增导读目录：`zh_docs/gnn_guide/`
  - `README.md`：阅读顺序、目标、关键代码索引
  - `01`：GNN 最小速成
  - `02`：本项目对象中心动力学与观测拆分
  - `03`：核心网络 `GraphNeuralNetwork` 读代码
  - `04`：`GNNForwardModel` 训练/预处理/归一化/损失
  - `05`：并行 ensemble GNN（`[E,B,...]` 维度）
  - `06`：如何运行与调参、常见坑

## 验证

- 文档生成后已在本地列出文件并检查 git status（未跟踪 -> 待提交）。

## 注意/回滚

- 纯文档改动；如需回滚，直接删除 `zh_docs/gnn_guide/` 与本记录即可。
