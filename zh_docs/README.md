# 本仓库中文文档（接手维护用）

这个目录用于存放我接手该项目之后的维护文档与决策记录（以中文为主）。

## 目录结构

- `changes/`：每次提交（commit）的变更记录（建议一条 commit 一份文件）

## 建议写法

- 以“能复现、能追责”为原则：写清楚动机、影响范围、验证方式。
- 变更记录优先记录“为什么这么改”“怎么验证”，其次才是细节。

## 约定

- 变更记录文件命名：`YYYY-MM-DD_<shortsha>_<topic>.md`
- 每条记录包含：背景、改动、验证、注意事项/回滚方式。
