env: "FetchPickAndPlaceConstruction"
env_params:
  # Backend selectors (default keeps current behavior).
  physics_backend: "mujoco"
  render_backend: "native"
  sparse: false
  shaped_reward: true
  num_blocks: 4
  stack_only: true
  case: "Singletower"
  visualize_target: false
  visualize_mocap: false
